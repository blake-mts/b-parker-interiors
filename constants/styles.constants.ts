export const FILL = { height: '100%', width: '100%' } as const;

export const CENTER = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
} as const;

export const MIN_HEIGHT_100 = {
    minHeight: '100%',
    height: '1px',
} as const;
